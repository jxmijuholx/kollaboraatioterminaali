import React, { useRef, useEffect, useState } from 'react';

const GameCanvas = ({ gameID}) => {
 
  // console.log("Game canvas PADELS", movePaddle);
  // console.log("Game canvas WS", ws);
  console.log("Game canvas gameID", gameID);
  // console.log("Game canvas username ", username);
  return (
    <div>
     <p>Tyhjä kanvas</p>
    </div>
  );
};

export default GameCanvas;
